_schema-version: 3.3.0
ID: Training1
description: A simple CAP project.
version: 1.0.0
modules:
- name: Training1-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: Training1-db
  - name: uaa_Training1
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: Training1-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: Training1-db
  parameters:
    buildpack: nodejs_buildpack
- name: Training1-destination-content
  type: com.sap.application.content
  requires:
  - name: Training1-destination-service
    parameters:
      content-target: true
  - name: Training1_html_repo_host
    parameters:
      service-key:
        name: Training1_html_repo_host-key
  - name: uaa_Training1
    parameters:
      service-key:
        name: uaa_Training1-key
  parameters:
    content:
      instance:
        destinations:
        - Name: training_Training1_html_repo_host
          ServiceInstanceName: Training1-html5-app-host-service
          ServiceKeyName: Training1_html_repo_host-key
          sap.cloud.service: training
        - Authentication: OAuth2UserTokenExchange
          Name: training_uaa_Training1
          ServiceInstanceName: Training1-xsuaa-service
          ServiceKeyName: uaa_Training1-key
          sap.cloud.service: training
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: Training1-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Training1_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - project1.zip
      name: project1
      target-path: resources/
- name: project1
  type: html5
  path: app/project1
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Training1-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: Training1-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: Training1-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: Training1_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: Training1-html5-app-host-service
    service-plan: app-host
- name: uaa_Training1
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: Training1-xsuaa-service
    service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
